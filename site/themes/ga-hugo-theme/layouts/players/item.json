      {
        "title": "{{ .Title }}",
        "postcode": "{{ .Params.postcode | plainify }}",
        "region" : "{{ .Params.region | safeHTMLattr }}",
        "road" : "{{ .Params.road | safeHtml }}",
        "district" : "{{ .Params.district | safeHTML }}",        
        {{ range $index, $e := .Site.Taxonomies }}{{ with $.Param $index }}
        "{{ $index }}" : "{{ delimit . ", "}}",{{ end }}{{ end }}
        "content": "{{ .Content | markdownify }}",
        "permalink" : "{{ .Permalink }}",
        "modified": "{{ .Lastmod }}"                                  
      }






